== Tic Tac Toe ==

Kindergarten implementation of tictactoe in python

# representation 

state is 9 zeros + 1 zero which tells whose turn is it

because `0` looks like `o` or `O` i'll represent O's as -1 and X's as 1, 0 means empty square


